<Window x:Class="MicroserviceManager.ServiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Service Configuration" Height="420" Width="600"
        Icon="app.ico"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5,10,5,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Service Name:" />
        <TextBox Grid.Row="1" x:Name="ServiceNameTextBox" />

        <Label Grid.Row="2" Content="Project Path:" />
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ProjectPathTextBox" />
            <Button Grid.Column="1" Content="Browse..." Click="Browse_Click" Background="#3498DB" Foreground="White"/>
        </Grid>

        <Label Grid.Row="4" Content="Run Command:" />
        <TextBox Grid.Row="5" x:Name="CommandTextBox" 
                VerticalAlignment="Stretch"
                TextWrapping="Wrap"
                AcceptsReturn="False"
                MinHeight="60"
                MaxHeight="120"
                VerticalScrollBarVisibility="Auto"
                Margin="5"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Click="Save_Click" Background="#27AE60" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Click="Cancel_Click" Background="#95A5A6" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>

